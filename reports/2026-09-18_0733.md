# XAU/USD signal tracker — analysis report
**Window:** repo creation → 2026-09-18 07:33 UTC (first report; no prior report exists, so the full history is in scope)

## Capture health

Nothing has been captured yet. This is the very first analysis run, and the pipeline has not produced a single successful frame.

- **Actions runs:** exactly **1** run of `capture.yml` has ever executed, triggered manually (`workflow_dispatch`) at **2026-09-18 06:15:43 UTC**. The job itself reports `success`, but that only means the Python process exited 0 — internally the capture attempt failed and the script swallows that so a failing 10-minute schedule doesn't spam email.
- **Scheduled runs:** the workflow is defined to run every 10 minutes (`3,13,23,33,43,53 * * * *`), but as of this report (about 1h17m after the file was added) **no scheduled run has fired at all** — only the one manual dispatch. GitHub can take up to ~an hour to start honoring a newly-added `schedule:` trigger on the default branch, so this may simply not have kicked in yet. Worth checking again next run; if scheduled runs still haven't started by then, the workflow file/branch state needs a look.
- **Error:** `data/capture_errors.log` has one entry, from the sole run:

  > `2026-09-18T06:16:04Z RuntimeError: no live XAU/USD stream found: ... ERROR: [youtube] 3H4IVQejlDE: Sign in to confirm you're not a bot. Use --cookies-from-browser or --cookies for the authentication. ...`

  This is exactly the **expired/missing YouTube-cookies** failure mode called out in the README: yt-dlp got the "Sign in to confirm you're not a bot" challenge on both the direct video URL and the channel's `/live` fallback. The `YT_COOKIES` Actions secret is either not set yet or already stale.
- **`data/capture_status.json`** does not exist yet — it should have been written alongside `capture_errors.log` on that failed run but wasn't committed. Not a blocker for now (there's nothing yet to act on: the failing-issue automation only fires after 6 consecutive failures with that file present), but if it's still missing after the next few runs, that's a separate small bug worth a look.
- No `data/trades.csv`, `data/events.csv`, `data/snapshots/`, or `frames` branch exist yet — there is nothing to verify against chart images this run.

## Trades in the window

None. No trade has been recorded, and there are no chart frames to cross-check for signals that closed in under 10 minutes either.

## Scoreboard

All-time: **0 trades**. No win rate, stop-out rate, reversal rate, or R figures can be computed yet. This section will populate once captures start succeeding.

## Current state

Unknown — no successful capture yet, so there's no live trade, price, or trend table to report.

## Verdict

No data to judge yet. **Action needed before this tracker can do anything:** populate the `YT_COOKIES` secret (Settings → Secrets and variables → Actions → `YT_COOKIES`) with a fresh Netscape-format cookies export from the throwaway YouTube account, per the README's refresh steps. Once that's in place and a scheduled run succeeds, the next report will have real captures — and, after enough trades accumulate, real signal-quality analysis. Nothing here is trading advice.
